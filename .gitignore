node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.*.local

*.log
npm-debug.log*

.DS_Store
Thumbs.db

# 本地上传文件(生产走对象存储)
/uploads/

# 采集到的院校原始数据 —— **纳入版本管理**(共 924KB)。
#
# 早先这里排除了 *.json,理由是「体积大」。但 924KB 根本不大,而代价很实在:
#   · git clone 下来跑起来前台是空的(没有任何院校项目)
#   · 部署只能靠 scp tar 包,不能 git pull
#   · 数据改了没有版本记录 —— 而这是「数据准确性是生命线」的产品,
#     恰恰最该能追溯谁在什么时候改了哪条
# 内容是各校官网公开的项目信息,不含任何个人数据。

next-env.d.ts
tsconfig.tsbuildinfo
.pgdata/

# 部署用的打包产物
compass.tar.gz
